export interface GoogleUser {
  email: string;
  name: string;
  picture: string;
}

export interface Product {
  id: string;
  price: number;
  name: string;
  description: string;
  images: string[];
}
